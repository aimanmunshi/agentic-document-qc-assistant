# Core LLM / agent framework
langchain>=0.2.0
langchain-anthropic>=0.1.23
langchain-core>=0.2.38

# Structured schemas
pydantic>=2.7.0

# Env management
python-dotenv>=1.0.1

# Testing
pytest>=8.2.0

# Note: langchain-anthropic pulls in the `anthropic` SDK automatically.
# If you prefer OpenAI instead of Anthropic, swap langchain-anthropic for
# langchain-openai and set OPENAI_API_KEY in .env instead of ANTHROPIC_API_KEY.
